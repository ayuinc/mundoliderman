{
  "name": "socketio",
  "version": "1.0.0",
  "description": "Web Socket Server para mundoliderman.com",
  "main": "index.js",
  "scripts": {
    "build": "babel server/index.js server/**/*.js --out-dir dist --presets es2015",
    "start": "NODE_PATH=. npm run build && node dist/server"
  },
  "author": "Hugo Angeles",
  "license": "MIT",
  "dependencies": {
    "babel-cli": "^6.6.5",
    "babel-preset-es2015": "^6.6.0",
    "express": "^4.13.4",
    "socket.io": "^1.4.5"
  }
}
